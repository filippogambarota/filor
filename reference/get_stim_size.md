# get_stim_size

calculating the visual

## Usage

``` r
get_stim_size(vis_angle, distance)
```

## Arguments

- vis_angle:

  stimulus size in degrees of visual angles

- distance:

  distance from the screen

## Value

the distance
