# leave1out

leave1out

## Usage

``` r
leave1out(data, group_var = NULL, keep_full = FALSE)
```

## Arguments

- data:

  a dataframe

- group_var:

  name of the variable that identify each observation to remove. If
  `NULL` will be removed one dataframe line at time.

## Value

a named list of dataframes
