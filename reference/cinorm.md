# cinorm

cinorm

## Usage

``` r
cinorm(x = NULL, se = NULL, avg = NULL, interval = 0.95)
```

## Arguments

- x:

  a vector

- se:

  the standard error

- avg:

  the mean

- interval:

  the desired interval

## Value

the upper and lower CI bound
