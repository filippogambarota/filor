# sourceR

Load one or more R functions from R/ folder (or any custom folder)

## Usage

``` r
sourceR(files = NULL, path = "R")
```

## Arguments

- files:

  files to source. Default to NULL (i.e., all files)

- path:

  path with .R files. Default to "R/" as in standard package-like
  projects.
