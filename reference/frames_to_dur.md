# frames_to_dur

frames_to_dur

## Usage

``` r
frames_to_dur(nframes, refresh_rate)
```

## Arguments

- nframes:

  the number of frames

- refresh_rate:

  the monitor refresh rate (e.g. 60)

## Value

the duration in ms
