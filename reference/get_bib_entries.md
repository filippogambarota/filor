# get_bib_entries

get_bib_entries

## Usage

``` r
get_bib_entries(file = knitr::current_input())
```

## Arguments

- file:

  character that indicate the input file. Default to the current `.Rmd`
  file if not specified.

## Value

character vector with all bibtex entry used in a document
