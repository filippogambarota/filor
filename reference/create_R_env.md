# create_R_env

Create an ".Renviron" file if not exists and optionally opens it.

## Usage

``` r
create_R_env(open = TRUE)
```

## Arguments

- open:

  logical indicating if opening the file interactively
