# get_chunk_labels

get_chunk_labels

## Usage

``` r
get_chunk_labels(file, output = NULL, as_captions = FALSE)
```

## Arguments

- file:

  the input rmd document

- output:

  the output text document. Default to NULL

- as_captions:

  logical. If the output should be formatted as captions
