# ncor

calculate the number of correlations given the size of the correlation
matrix. In practice is the same as counting the number of elements in
the upper/lower triangle of the correlation matrix

## Usage

``` r
ncor(p)
```

## Arguments

- p:

  integer for the size of the correlation matrix

## Value

the number of correlations
