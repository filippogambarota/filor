# cap_link

Format html code for an hyperlink

## Usage

``` r
cap_link(link, text = NULL)
```

## Arguments

- link:

  character. The web address

- text:

  charactet. The text to be displayed. Default to `NULL` thus the same
  as the `link`

## Value

The html code for the hyperlink
