# dur_to_frames

return the number of frames associated with a specific duration (in ms)

## Usage

``` r
dur_to_frames(duration, refresh_rate)
```

## Arguments

- duration:

  the duration in ms

- refresh_rate:

  the monitor refresh rate (e.g. 60)

## Value

the number of frames
